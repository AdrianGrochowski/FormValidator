# FormValidator

Javascript form validator in one class. 
Complex validation of a login form aswell as shipping form. 

In constructor we pass the container where the inputs are, the inputs itself, function with a regular expression to validate input, fontawesome icon to be changed and the message to be displayed in a tooltip in case of bad input. 
